# =============================================================================
# requirements.txt
# An Auditable AI Agent Loop for Empirical Economics: A Case Study in Forecast Combination
#
# Author: CS Chirinda
# Paper Reference: Shin, M. (2026). arXiv:2603.17381v2
# Python Version: 3.10+
# Last Updated: 2026-03-22
#
# Version Pinning Philosophy:
# • Exact pins (==) for packages where numerical reproducibility is critical
# • Compatible release (~=) for infrastructure packages where patch updates are safe
# • All versions tested against the ECB SPF replication study
#
# Installation:
#   pip install -r requirements.txt
#
# Verification:
#   pip freeze > installed.txt && diff requirements.txt installed.txt
# =============================================================================

# -----------------------------------------------------------------------------
# CORE SCIENTIFIC COMPUTING STACK
# Foundational packages for high-dimensional data manipulation and linear algebra
# These versions ensure bit-exact reproducibility of penalized regression estimators
# -----------------------------------------------------------------------------

# NumPy: Fundamental package for numerical computing
# • Required for: All matrix operations, rolling-origin evaluation, forecast aggregation
# • Critical for: Ridge/LASSO weight calculations (Equation 8 in paper)
# • Pinned exactly: Floating-point operations must be deterministic across platforms
numpy==1.26.4

# Pandas: High-performance data structures for time series manipulation
# • Required for: SPF microdata ingestion, temporal partitioning, missing value imputation
# • Critical for: Rolling window construction (W=20), temporal weighting (α=6)
# • Pinned exactly: DataFrame indexing and aggregation semantics must match original study
pandas==2.2.1

# SciPy: Scientific computing library for advanced statistics
# • Required for: Diebold-Mariano test (scipy.stats.norm for HAC variance estimation)
# • Critical for: EWC fixed-b approximation (Shin and Schor, 2026)
# • Pinned exactly: Statistical distributions must produce identical p-values
scipy==1.12.0

# -----------------------------------------------------------------------------
# MACHINE LEARNING AND REGULARIZED REGRESSION
# Implements the penalized regression estimators central to forecast combination
# -----------------------------------------------------------------------------

# Scikit-learn: Machine learning library for penalized regression
# • Required for: LASSO, Ridge, ElasticNet, Adaptive LASSO
# • Critical for: Partially-Egalitarian LASSO (peLASSO) two-step estimation
# • Critical for: Leave-One-Out cross-validation for bias correction (γ=0.80)
# • Pinned exactly: Coordinate descent convergence tolerances affect λ path
# • Note: sklearn.linear_model.{Lasso, Ridge, LassoCV, RidgeCV, ElasticNetCV, enet_path}
scikit-learn==1.4.1.post1

# -----------------------------------------------------------------------------
# LLM API ORCHESTRATION AND AGENTIC SEARCH
# Manages the stochastic optimizer (Claude) within the deterministic protocol
# -----------------------------------------------------------------------------

# Anthropic SDK: Official Python client for Claude API
# • Required for: LLM-driven code generation, autonomous session recovery
# • Critical for: Instruction contract (C) execution, evaluator-optimizer loop
# • Compatible release: API contract is stable, patch updates contain bug fixes only
# • Minimum version: 0.18.0 (introduces streaming and tool use features)
anthropic>=0.18.0,<1.0.0

# -----------------------------------------------------------------------------
# TRANSITIVE DEPENDENCIES (Explicit Declaration for Audit Trail)
# While installed automatically, we declare them for version-lock transparency
# -----------------------------------------------------------------------------

# Typing Extensions: Backport of typing features for Python 3.10
# • Required by: anthropic SDK, internal type hints
# • Compatible release: Typing semantics are forward-compatible
typing-extensions>=4.9.0

# Requests: HTTP library for web API interactions
# • Required by: anthropic SDK (API calls)
# • Compatible release: HTTP semantics are stable
requests>=2.31.0,<3.0.0

# Certifi: Mozilla's SSL certificate bundle
# • Required by: requests (HTTPS verification)
# • Compatible release: Certificate updates are security-critical but API-stable
certifi>=2024.2.2

# Charset-normalizer: Character encoding detection
# • Required by: requests (response decoding)
# • Compatible release: Encoding detection is deterministic
charset-normalizer>=3.3.0,<4.0.0

# Idna: Internationalized Domain Names in Applications
# • Required by: requests (URL encoding)
# • Compatible release: IDNA specification is stable
idna>=3.6,<4.0

# Urllib3: HTTP client library
# • Required by: requests (connection pooling)
# • Compatible release: HTTP/1.1 semantics are stable
urllib3>=2.2.0,<3.0.0

# Distro: Linux distribution identification
# • Required by: anthropic SDK (system diagnostics)
# • Compatible release: OS detection is informational only
distro>=1.9.0

# Anyio: Asynchronous I/O library
# • Required by: anthropic SDK (async API calls)
# • Compatible release: Async semantics are stable
anyio>=4.3.0,<5.0.0

# Sniffio: Async library detection
# • Required by: anyio (runtime detection)
# • Compatible release: Detection logic is stable
sniffio>=1.3.0

# Pydantic: Data validation using Python type hints
# • Required by: anthropic SDK (API response validation)
# • Compatible release: Validation logic is stable, v2.x is API-stable
pydantic>=2.6.0,<3.0.0

# Pydantic-core: Core validation logic for Pydantic
# • Required by: pydantic (Rust-accelerated validators)
# • Compatible release: Tied to pydantic version
pydantic-core>=2.16.0,<3.0.0

# Annotated-types: Reusable constraint types for Pydantic
# • Required by: pydantic (type annotations)
# • Compatible release: Annotation semantics are stable
annotated-types>=0.6.0

# -----------------------------------------------------------------------------
# NUMERICAL COMPUTING SUPPORT LIBRARIES
# Low-level dependencies that ensure numerical stability
# -----------------------------------------------------------------------------

# Joblib: Lightweight pipelining for parallel computing
# • Required by: scikit-learn (cross-validation parallelization)
# • Compatible release: Parallel execution semantics are stable
joblib>=1.3.2

# Threadpoolctl: Thread pool management for BLAS/LAPACK
# • Required by: scikit-learn (NumPy thread control)
# • Compatible release: Thread management is transparent
threadpoolctl>=3.2.0

# Python-dateutil: Extensions to the standard datetime module
# • Required by: pandas (datetime parsing)
# • Compatible release: Date parsing is deterministic
python-dateutil>=2.8.2

# Pytz: World timezone definitions
# • Required by: pandas (timezone-aware timestamps)
# • Compatible release: IANA timezone database updates are backward-compatible
pytz>=2024.1

# Tzdata: IANA timezone database (Windows fallback)
# • Required by: pandas on Windows (timezone data)
# • Compatible release: Timezone updates are backward-compatible
tzdata>=2024.1

# Six: Python 2 and 3 compatibility utilities
# • Required by: python-dateutil (legacy compatibility layer)
# • Exact pin: Deprecated but stable, no updates expected
six==1.16.0

# -----------------------------------------------------------------------------
# DEVELOPMENT AND TESTING DEPENDENCIES (Optional)
# Uncomment for local development, code quality checks, and unit testing
# -----------------------------------------------------------------------------

# # MyPy: Static type checker for Python
# # • Purpose: Verify type hint correctness in train.py and prepare.py
# mypy==1.8.0
#
# # Pytest: Testing framework
# # • Purpose: Unit tests for penalized regression implementations
# pytest==8.0.0
#
# # Pytest-cov: Coverage plugin for pytest
# # • Purpose: Ensure test coverage of critical paths (e.g., LOO-CV)
# pytest-cov==4.1.0
#
# # Black: Opinionated code formatter
# # • Purpose: Enforce PEP-8 compliance
# black==24.2.0
#
# # Flake8: Style guide enforcement
# # • Purpose: Catch syntax errors and style violations
# flake8==7.0.0
#
# # Isort: Import statement organizer
# # • Purpose: Maintain canonical import order
# isort==5.13.2

# -----------------------------------------------------------------------------
# PLATFORM-SPECIFIC NOTES
# -----------------------------------------------------------------------------
# 
# CUDA/GPU Support:
#   This requirements.txt intentionally excludes PyTorch/CUDA dependencies
#   mentioned in the paper's Transformer decoder context. The econometric
#   case study (Sections 3-5) operates entirely on CPU using sklearn.
#   For GPU-accelerated LLM fine-tuning (Section 2), see separate torch.txt.
#
# Windows Users:
#   Ensure Visual C++ 14.0+ is installed for scikit-learn compilation.
#   Download from: https://visualstudio.microsoft.com/visual-cpp-build-tools/
#
# macOS Users (Apple Silicon):
#   NumPy/SciPy use Accelerate framework by default (not OpenBLAS).
#   For reproducibility across platforms, consider forcing OpenBLAS:
#     brew install openblas
#     OPENBLAS="$(brew --prefix openblas)" pip install -r requirements.txt
#
# Linux Users:
#   For optimal BLAS performance, install system-level libraries:
#     Ubuntu/Debian: sudo apt-get install libblas-dev liblapack-dev
#     RHEL/CentOS: sudo yum install blas-devel lapack-devel
#
# =============================================================================
# END OF FILE
# =============================================================================